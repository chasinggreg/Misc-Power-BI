1️⃣ Subtitle (Total Count of "Yes" Orders)

IsOrderOlderThan18M_Count =
VAR YesCount = COUNTROWS(
    FILTER('Snapshot', 'Snapshot'[Is Order Older Than 18 Months] = "Yes")
)
VAR Icon = IF(YesCount > 0, "⚠️", "✅") -- Warning if issues exist, checkmark if none
RETURN
    Icon & " " & FORMAT(YesCount, "#,##0")



👉 Example Output: 🟠 275


2️⃣ Callout Value (Percentage of "Yes" Orders)

IsOrderOlderThan18M_Percent =
VAR YesCount = COUNTROWS(
    FILTER('Snapshot', 'Snapshot'[Is Order Older Than 18 Months] = "Yes")
)
VAR TotalOrders = DISTINCTCOUNT('Snapshot'[Doc Nbr])
RETURN
IF(
    TotalOrders = 0, BLANK(), -- Avoid divide by zero errors
    FORMAT(YesCount / TotalOrders, "0.0%")
)


👉 Example Output: 🟢 12.5%
